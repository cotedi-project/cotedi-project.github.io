<!DOCTYPE html>
<html lang="{{ meta.lang or site.lang }}">
  {% include "blocks/html_head.njk" %}

  <body>
    <!-- skiplinks -->
    <a href="#main" class="visually-hidden-focusable">Skip to main content</a>

    {% include "header.njk" %}  

    <main id="main">
      {% if title %}<div class="container py-4">
        <h1>{{ title or meta.title }}</h1>
      </div>{% endif %}
      <div class="container py-4">
        {{ content | safe }}
      </div>
    </main>

    {% include "footer.njk" %}

  </body>
</html>
